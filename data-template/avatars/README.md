# data/avatars/

學生部落格主頁的頭像（選用）。**檔名只准用座號**：`01.jpg`、`02.png`……（不要用孩子的名字當檔名）。
`scripts/open_blogs.py` 會把它縮成小圖（最長邊 320、清掉 EXIF 與 GPS）放進部落格主頁；
換頭像＝換掉這裡的檔，再跑一次 `open_blogs.py`（先預覽，再 `--apply`）。沒有頭像就不用放任何檔。
